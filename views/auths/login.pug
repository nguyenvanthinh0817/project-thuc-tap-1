extends ../layouts/common

block content
	h1 Login
	//- if errors
	//- 	each error in errors
	//- 		.alert.alert-danger= error
	form(action='/auth/postLogin', method='POST')
		.form-group
			label(for="email") Email
			input(type='text', name='email',id="email", class="form-control mb-3", value=values ? values.email : '')
			label(for="password") Password
			input(type='text', name='password',id="password", class="form-control mb-3", value=values ? values.password : '')
			button.btn.btn-success Login